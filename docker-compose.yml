version: "3.9"

services:
  db:
    image: postgres:16-alpine
    container_name: pgsql-lite
    restart: always
    environment:
      POSTGRES_USER: pine_tick
      POSTGRES_PASSWORD: password
      POSTGRES_DB: pine_tick
      TZ: Asia/Shanghai
    ports:
      - "25432:5432"
